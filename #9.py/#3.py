# numa fila os elementos são adicionados por ordem de chegada, e sempre no final, chmamos isso de FIFO(first in, first out)

# o pop é uma função que tira o valor da lista, mas que retorna o valor do elemento

a = [10, 11, 12]

b = a.pop(0) # eu posso salvar o valor

print(a)

print(b)